class Comment {
  constructor({ id, blogId, name, email, body }) {
    this.id = id;
    this.blogId = blogId;
    this.name = name;
    this.email = email;
    this.body = body;
  }
}

export default Comment;
